# Skill Registry for thewestrep/login-2

> Auto-generated by SDD Init on 2026-04-29 (HYBRID mode)

## User Skills (Available)

| Skill | Purpose | Auto-load Trigger |
|-------|---------|-------------------|
| sdd-init | Initialize SDD context | `sdd init` |
| sdd-explore | Explore and investigate ideas | `/sdd-explore <topic>` |
| sdd-propose | Create change proposals | `/sdd-propose <change>` |
| sdd-spec | Write specifications | `/sdd-spec <change>` |
| sdd-design | Create technical design | `/sdd-design <change>` |
| sdd-tasks | Break down into tasks | `/sdd-tasks <change>` |
| sdd-apply | Implement changes | `/sdd-apply <change>` |
| sdd-verify | Verify implementation | `/sdd-verify <change>` |
| sdd-archive | Archive completed changes | `/sdd-archive <change>` |
| sdd-onboard | Guided SDD walkthrough | `/sdd-onboard` |
| branch-pr | PR creation workflow | Creating PRs |
| issue-creation | GitHub issue creation | Creating issues |
| judgment-day | Adversarial review | `judgment day` |
| go-testing | Go testing patterns | Go test files |
| skill-creator | Create new AI skills | Creating skills |
| skill-registry | Update this registry | `update skills` |

## Project Skills (Local)

| Skill | Purpose | Auto-load Trigger |
|-------|---------|-------------------|
| git-delivery | Safe commit, push, and PR workflow using gga | Committing, pushing, creating PRs, or preparing changes for review |
| workspace-bootstrap | Orca workspace creation with isolated branches/worktrees | `new workspace`, `crear workspace`, `arrancar tarea`, `branch nueva` |

## Compact Rules

### go-testing
- Use `teatest` for Bubbletea TUI testing — test the final rendered output
- Run tests with `go test ./...` from module root
- Use `testscript` for CLI integration tests
- Prefer table-driven tests with subtests
- Mock external dependencies with interfaces, not function variables

### git-delivery
- Inspect `git status`, `git diff`, `git diff --staged`, and recent `git log` before committing, pushing, or opening PRs.
- Never commit secrets; stop on `.env`, credentials, keys, tokens, dumps, or private config.
- Use conventional commits only; never add AI attribution or `Co-Authored-By` trailers.
- Do not run builds as routine verification; prefer `npm run lint`, `npm run typecheck`, and relevant tests.
- Run `gga run` on staged changes before committing; fix, re-stage, and re-run until clean.
- Run `gga run --pr-mode --diff-only` before creating PRs; use full `--pr-mode` when small PRs need full-file context.
- Use `gh` for PR creation/checks and push safely; never force-push unless explicitly requested, never to `main`/`master`.

### workspace-bootstrap
- One workspace = one branch = one task
- Always start from updated `main`
- Never code on `main` or `master`
- Prefer `git worktree` for parallel workspaces
- Use clear branch names: `feature/`, `fix/`, `refactor/`, `chore/`

## Project Conventions

### From AGENTS.md
| File | Path | Notes |
|------|------|-------|
| AGENTS.md | AGENTS.md | Index — references git-delivery skill, Next.js 16 breaking changes warning |
| CLAUDE.md | CLAUDE.md | References AGENTS.md |
| .codex/config.toml | .codex/config.toml | Codex MCP config (Resend) |
| opencode.json | opencode.json | OpenCode config (Resend MCP) |
| skills/git-delivery/SKILL.md | skills/git-delivery/SKILL.md | Project skill for gga delivery workflow |
| skills/workspace-bootstrap/SKILL.md | skills/workspace-bootstrap/SKILL.md | Project skill for Orca workspace setup |

### Critical Rules (extracted)
- **CRITICAL**: This is NOT the Next.js you know — APIs, conventions, and file structure differ from training data
- **ALWAYS** read the relevant guide in `node_modules/next/dist/docs/` before writing any code
- Heed deprecation notices
- **CRITICAL**: Load `skills/git-delivery/SKILL.md` before commits, pushes, or pull requests
- Use `gga run` before committing staged changes and `gga run --pr-mode --diff-only` before opening PRs
- Do not run builds as part of routine verification
- Conventional commits only, no AI attribution, no `Co-Authored-By`

## Tech Stack Context

- Next.js 16.2.1 App Router (breaking changes from training data)
- React 19.2.4
- TypeScript strict mode
- TailwindCSS 4 (PostCSS plugin)
- Drizzle ORM + Neon PostgreSQL
- NextAuth 4 (auth)
- Zod 4 (validation)
- MercadoPago SDK (payments)
- Resend (email)
- Clean Architecture in lib/
- Native Node test runner (node:test)
- Playwright for E2E

## Auto-load Rules

```yaml
- pattern: "**/*.test.ts"
  skills: []  # TypeScript tests use node:test, not go-testing

- pattern: "**/*.spec.ts"
  skills: []  # Playwright specs

- pattern: "*.go"
  skills: [go-testing]

- pattern: "lib/**/*.ts"
  context: Clean Architecture patterns, server-only boundaries

- pattern: "app/**/*.tsx"
  context: Next.js App Router, Server Components

- pattern: "commit|push|PR|pull request|preparing changes for review"
  skills: [git-delivery]
```

## Relevant Files

- `AGENTS.md` — Project-specific agent rules (Next.js 16 breaking changes + git-delivery auto-load)
- `CLAUDE.md` — References AGENTS.md
- `openspec/config.yaml` — SDD configuration
- `skills/git-delivery/SKILL.md` — Project skill for safe gga commit/push/PR workflow
- `skills/workspace-bootstrap/SKILL.md` — Project skill for Orca workspace creation
- `.atl/skill-registry.md` — This registry